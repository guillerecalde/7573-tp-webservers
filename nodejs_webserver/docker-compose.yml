version: '2'
services:
  node:
    build: .
    # This line maps the contents of the client folder into the container.
    volumes:
      - .:/usr/src/app
  nginx:
    build: ./nginx
    ports:
      - "80:80"
    links:
      - node